extends layout

block content
  .container-fluid.d-flex.flex-column.align-items-center.min-vh-100.text-center.justify-content-center.gap-3
      h2 Log in
      form.d-flex.flex-column.gap-3.w-25(method="POST", action='/log-in')
        .form-group
          label(for="email") Email address
          input.form-control(value=(user ? user.email : ""), type="email", id="email", placeholder="Enter email", name="username")
        
        .form-group
          label(for="password") Password
          input.form-control(type="password", id="password", placeholder="Enter password", name="password")
        
        button.btn.btn-primary(type="submit") Submit
        if errors
          div
            each error in errors
              p.text-danger #{error.msg}
        if error
          h2= error.status
          pre #{error.stack}
        if messages.error
            p.text-danger #{messages.error[0]}